###############################################
# INTEGRATION TASK SUITE
###############################################

run:
	$(info ~~~~~~~~~~ RUN ~~~~~~~~~)
	npm start

install:
	$(info ~~~~~~~~~~ INSTALLING DEPENDENCIES ~~~~~~~~~)
	npm install
